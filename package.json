{
  "name": "transparent-overlay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "build:css": "npx @tailwindcss/cli -i ./src/tailwind.css -o ./src/styles.css --minify",
    "dev:css": "npx @tailwindcss/cli -i ./src/tailwind.css -o ./src/styles.css --watch"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "tailwindcss": "^4.0.17"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.17",
    "@tauri-apps/plugin-store": "^2.2.0",
    "daisyui": "^5.0.9"
  }
}
